# driverWeb-data
This repo contains the raw JSON driver data that is parsed and rendered via [DriverWeb](https://github.com/BurntToasters/driverWeb).
Each release may update the SCHEMA and in such is **NOT** backwards-compatible with previous versions of DriverWeb.
